# Core runtime dependencies for the cHAP scraper + analytics pipeline.
# Pinned to versions that were current as of 2026-04-18; bump with care.

playwright>=1.45.0,<2.0.0
python-dotenv>=1.0.1,<2.0.0
supabase>=2.7.0,<3.0.0
pandas>=2.2.0,<3.0.0
# Required when the runtime environment uses a SOCKS proxy (CI, sandboxed
# shells). supabase-py's internal httpx client errors at construction time
# without this even if your connection is plain HTTPS.
httpx[socks]>=0.27.0

# Dashboard (Phase 5) — required for `streamlit run dashboard.py`. Plotly
# backs the Install Trend / Installs-vs-Uninstalls / stacked-bar charts
# introduced in the Apr 2026 redesign (see project_chap_dashboard_redesign
# memory note).
#
# Bumped to 1.42+ for native st.login() (Google OIDC) used by auth.py.
# See MULTI_APP_DESIGN.md §3.3 for the [auth] secrets block.
streamlit>=1.42.0,<2.0.0
# Authlib is NOT a transitive dep of streamlit — st.login() raises
# StreamlitAuthError at runtime when it's missing. Must be pinned here
# so the Streamlit Cloud build installs it alongside streamlit itself.
Authlib>=1.3.2,<2.0.0
plotly>=5.20.0,<7.0.0

# Multi-app + RBAC (Apr 2026).
# - pyyaml: reads apps.yaml / roles.yaml
# - pynacl: libsodium-encrypts the CREDS secret before PUTting it via
#   the GitHub REST API from the admin UI
# - requests: GitHub REST API calls from admin UI + streamlit-oauth
#   fallback. Already pulled in transitively by other deps but pinned
#   here so admin_ui / github_secret_updater don't break silently.
# - streamlit-oauth: fallback OAuth path for when native st.login()
#   isn't available (e.g. self-hosted Streamlit below 1.42). Harmless
#   if unused.
pyyaml>=6.0,<7.0
pynacl>=1.5.0,<2.0.0
requests>=2.31.0,<3.0.0
streamlit-oauth>=0.1.14,<1.0.0

# Dev dependencies — not imported by the runtime pipeline. Uncomment
# when setting up a contributor venv:
#   pip install -r requirements.txt pytest
# Test suite lives in tests/ and is pure-Python (no Playwright, no
# network), so `python3 -m pytest tests/` is the one-liner to run it.
# pytest>=8.0,<9.0
